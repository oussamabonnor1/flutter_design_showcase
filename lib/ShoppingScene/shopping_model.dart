import 'package:flutter/material.dart';

class ShoppingModel{

  String title;
  String info1, info2;
  double price;
  AssetImage productImage;


  ShoppingModel(this.title, this.price, this.productImage, this.info1, this.info2);

}